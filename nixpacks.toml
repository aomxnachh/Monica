[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg", "yt-dlp", "libsodium", "python3", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["YOUTUBE_DL_SKIP_PYTHON_CHECK=1 npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/main.js"
